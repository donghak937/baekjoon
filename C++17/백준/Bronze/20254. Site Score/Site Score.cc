#include <iostream>

using namespace std;

int main() {

    long long a, b, c, d;

    cin >> a >> b >> c >> d;

    long long total = (a * 56) + (b * 24) + (c * 14) + (d * 6);

    cout << total;
}